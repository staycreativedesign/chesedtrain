.form-group
  = f.label field.to_sym, label, class: 'w-full-label'
  = f.text_field field.to_sym, class: 'w-full-field'
  - if event.errors[field.to_sym].any?
    .text-danger
      = event.errors[field.to_sym].join(", ")

